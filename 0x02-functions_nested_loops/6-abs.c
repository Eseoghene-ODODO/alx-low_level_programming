#include "main.h"
#include <stdio.h>

/**
 * _abs -> Function that computes the absolute value of an integer.
 *
 * @y: character
 *
 * Return: 0
 */

int _abs(int y)
{
	if (y > 0)
	{
		_putchar(y + '0');
	}
	else if (y == 0)
	{
		_putchar(y + '0');
	}
	else
	{
		_putchar(y + '0');
	}
	return (0);
}
